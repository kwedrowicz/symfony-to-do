vich_uploader:
    db_driver: orm

    mappings:
        task_image:
            uri_prefix:         /images/tasks
            upload_destination: '%kernel.root_dir%/../web/images/tasks'
            namer:              vich_uploader.namer_origname
